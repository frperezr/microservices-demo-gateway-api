module.exports = {
  id: ({ id }) => id,
  continentCode: ({ continentCode }) => continentCode,
  code: ({ code }) => code,
  name: ({ name }) => name,
  createdAt: ({ createdAt }) => createdAt,
  updatedAt: ({ updatedAt }) => updatedAt,
}
