module.exports = {
  id: ({ id }) => id,
  code: ({ code }) => code,
  name: ({ name }) => name,
  imageUrl: ({ imageUrl }) => imageUrl,
  createdAt: ({ createdAt }) => createdAt,
  updatedAt: ({ updatedAt }) => updatedAt,
}
