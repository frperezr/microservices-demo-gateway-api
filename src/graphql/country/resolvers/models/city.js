module.exports = {
  id: ({ id }) => id,
  stayPrice: ({ stayPrice }) => stayPrice,
  travelPrice: ({ travelPrice }) => travelPrice,
  country: ({ country }) => country,
  name: ({ name }) => name,
  createdAt: ({ createdAt }) => createdAt,
  updatedAt: ({ updatedAt }) => updatedAt,
}
